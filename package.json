{
  "name": "drone-sightings",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive Europe-wide drone incident map with automated data collection",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp public/incidents.json dist/incidents.json && cp public/incidents.schema.json dist/incidents.schema.json && cp public/favicon.svg dist/favicon.svg",
    "build-assets": "python3 tools/build_assets.py",
    "ingest": "python3 tools/ingest.py",
    "automation": "node automation/index.js run",
    "automation:start": "node automation/index.js start",
    "automation:run": "node automation/index.js run",
    "automation:continuous": "node automation/continuous-monitor.js",
    "automation:test": "node automation/index.js test",
    "automation:audit": "node automation/index.js audit",
    "automation:status": "node automation/index.js status",
    "collect": "npm run automation && npm run build"
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2",
    "xml2js": "^0.6.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "drone",
    "incident",
    "tracking",
    "automation",
    "europe",
    "security",
    "aviation"
  ]
}
